spring:
  profiles:
    active: test
  main:
   banner-mode: 'off'
   lazy-initialization: true
  jpa:
    show-sql: true
    open-in-view: false
logging:
  level:
    org.hibernate.SQL: DEBUG
    #org.hibernate.type.descriptor.sql.BasicBinder: TRACE